import type { IHeadlinePart } from '@/interfaces/i-headline-part';

export const heroHeadlineParts: IHeadlinePart[] = [
  { text: 'A resume builder that ', italic: false },
  { text: 'respects', italic: true },
  { text: ' the document.', italic: false },
];

export const heroTypewriterCharMs = 50;
export const heroTypewriterStartDelayMs = 320;
